class Solution:
    # @param A : tuple of integers
    # @return an integer
    def repeatedNumber(self, A):
        a=[0]*(len(A)-1)
        for i in A:
            if(a[i-1]):
                return i
            a[i-1]=1    
