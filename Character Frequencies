class Solution:
    # @param A : string
    # @return a list of integers
    def solve(self, A):
        a=[]
        ans=[]
        for i in A:
            if i not in a:
                a.append(i)
        for j in a:
            ans.append(A.count(j))
        return ans            
  
